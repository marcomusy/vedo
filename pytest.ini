[pytest]
addopts = -ra
testpaths = tests
norecursedirs = build dist develop docs .git .pytest_cache
markers =
    unit: fast unit-level tests
    integration: integration tests across modules
    interactive: tests requiring interactive rendering/input
